<%_if(getvar('stat_data.事件链.开启', { defaults: false }) === true){_%>
<!-- checkPoint -->
检查<event_chain>是否存在

若不存在
检索上文，{{user}}是否与当前事件有关
当前事件为:{{get_message_variable::stat_data.事件链.标题}}

若<event_chain>不存在且与事件无关
在本轮的<UpdateVariable>中输出:
  _.set('事件链.结束','true');
<%}%>